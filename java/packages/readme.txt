RUN BY --  java package_name.class_name

because all the classes inside p1 are accessed through the package name 

all the classes from a package should be there inside one folder only which should have the same name as the PackageName

Always execute a package from its parent directory 
